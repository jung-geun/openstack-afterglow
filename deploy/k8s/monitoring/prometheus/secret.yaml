apiVersion: v1
kind: Secret
metadata:
  name: prometheus-sd-token
  namespace: afterglow
type: Opaque
stringData:
  # monitoring_sd_token 환경변수 값과 동일하게 설정할 것
  sd-token: "REPLACE_WITH_MONITORING_SD_TOKEN"
