apiVersion: v1
kind: Service
metadata:
  name: redis-sentinel
  namespace: afterglow
  labels:
    app: redis-sentinel
spec:
  type: ClusterIP
  selector:
    app: redis-sentinel
  ports:
    - port: 26379
      targetPort: 26379
      name: sentinel
