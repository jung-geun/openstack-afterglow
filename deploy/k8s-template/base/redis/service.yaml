apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: afterglow
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis
    statefulset.kubernetes.io/pod-name: redis-0
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
