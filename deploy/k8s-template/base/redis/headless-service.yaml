apiVersion: v1
kind: Service
metadata:
  name: redis-headless
  namespace: afterglow
  labels:
    app: redis
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
