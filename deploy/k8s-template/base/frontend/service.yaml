apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: afterglow
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 3080
      targetPort: 3080
      protocol: TCP
