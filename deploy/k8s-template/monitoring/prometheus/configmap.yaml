apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: afterglow
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'afterglow-api'
        static_configs:
          - targets: ['backend:8000']
        metrics_path: '/api/metrics'
