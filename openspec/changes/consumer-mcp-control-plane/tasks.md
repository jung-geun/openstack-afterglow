## Implementation Tasks

- [x] 1. Record the change and preserve rollout isolation: retained `mcp==1.28.1`, verified `dev`, preserved the pre-existing dirty worktree, and added the disabled `[services].mcp = false` rollout gate to config loading, generated K8s TOML, and both examples. Cross-change evidence is recorded in `builtin-ai-chat`; no shared chat task is marked complete.
- [x] 2. Add the dedicated delegated-authority model: normalized tables, strict AEAD, owner-locked issuance/revocation/selection, browser management APIs, and the complete lease-guarded cleanup sweep. `backend/tests/test_mcp_credentials.py` covers AEAD/PAT/scoped app credentials/orphan naming/route precedence; real-MariaDB coverage proves OAuth refresh replay, expiry terminalization, Lumen-selection invalidation, canonical `authorize_mutation_dispatch()` owner-lock serialization, confirmed upstream deletion reconciliation when a lease appears between local phases, and idempotent concurrent cleanup confirmation. Owner-lock bootstrap now uses a single MySQL upsert before `SELECT … FOR UPDATE`, with concurrent first-owner coverage. The migration-ledger target remains independently blocked by pre-existing checksum drift in migration `016`.
- [x] 3. Host a standards-compliant OAuth 2.1 authorization server in Stage 2: exact HTTPS resource/issuer metadata, DCR/CIMD validation, PKCE S256 authorization code plus rotating refresh tokens, strict resource/scope/redirect checks, and fail-closed revocation/refresh races. Unit and ASGI transport coverage verifies exact resource/PKCE/redirect validation; closed public-client DCR metadata; one-time public registration with no secret persistence or output; caller-chosen client-ID rejection; no-store/no-referrer response headers; form, origin, and HTTP Basic/client-secret rejection at token/revocation; consent no-store headers; and configured-path metadata. Real MariaDB verifies refresh replay revokes the durable family and delegated grant.
- [x] 4. Mount the current MCP transport with fail-closed request context: official stateless Streamable HTTP SDK transport at exact `/api/v1/mcp`, independent authentication, origin/host/media negotiation, bounded pagination/rates/concurrency, and explicit lifecycle management. API, root-discovery/alias, and consent routers now keep relative paths and are mounted only in `main.py` under their v1 prefixes. `test_mcp_server.py` verifies lifecycle, default/configured path CORS isolation, cursor, and preview boundaries; `test_mcp_limits.py` verifies per-grant limits and Redis failure fail-closed behavior.
- [x] 5. Create one canonical consumer-only registry and shared domain boundary: versioned closed schemas, grant-only principals, consumer adapters with strict ownership proofs, previews, safe outputs, audit, and no reflected HTTP/provider bypasses. The closed registry, mutation envelope, bounded serialization/timeouts, operation reader, grant-scoped connection factory, and strictly owned Nova/Cinder/Neutron/Trove/Zun adapters pass the focused 45-test domain selector.
- [x] 6. Deliver the Stage 1 internal core cloud catalog: capability/operation/dashboard tools plus strictly owned Nova, Cinder, Neutron, Trove, and Zun read/mutation tools with idempotency, previews, redaction, audit, and policy fixes; keep public service disabled. The focused 45-test domain selector verifies the internal safe catalog; `services.mcp=false` remains the public publish gate.
- [x] 7. Make mutation replay and asynchronous state explicit: validated idempotency keys and canonical argument hashing, atomic ledger claims/replay/tombstones, unknown send-boundary handling, owner-scoped operation reconciliation, and final preview/precondition revalidation. Focused registry/domain/transport coverage verifies durable claims, canonical idempotency, current-state previews before dispatch, pre-dispatch failure persistence, replay-safe mutation boundaries, and safe operation reads.
- [x] 8. Reuse the registry inside Lumen with durable v2 approval: server-owned registry provider, frozen opaque grant/selection snapshots, encrypted PostgreSQL v2 checkpointing, HMAC-bound typed approvals, final revalidation, and frontend chat approval lifecycle support. Focused backend protocol/graph coverage (54 tests) verifies frozen Lumen selection, no re-selection during dispatch, registry/ledger reuse, fail-closed v2 admission, typed approvals, and final binding checks. Three real MariaDB/PostgreSQL cases verify encrypted checkpoint initialization plus persisted HMAC-bound approval interruption/resume and duplicate-interrupt rollback. Chat approval component coverage adds 9 passing Vitest checks. Version 2 remains explicitly opt-in; version 1 remains the default rollout configuration.
- [x] 9. Deliver the Stage 2 internal catalog and OAuth completion: close policy gaps and register every specified safe consumer service subset; omit disabled services and fail the inventory gate for unclassified consumer cloud routes. The inventory gate classifies Nova/Cinder/Neutron (including subnet)/Trove/Zun plus explicit Manila quota, K3s cluster metadata, Swift aggregate, Barbican secret-metadata, and Waygate server-metadata domains. Stage 2 tools are read-only, service-gated, project-scoped, bounded at their provider/database source, and schema-redacted; secret payloads, ACLs, exports, K3s credentials/workload APIs, object payloads, and Waygate connection/key/agent data remain deliberately absent.
- [x] 10. Add account, OAuth-consent, and Lumen tutorial UX: account token/grant management, secure one-time plaintext handling, consent route with ticket binding and secure headers, Lumen starter prompts, and deterministic mock/browser coverage. The deterministic account mock returns the issued plaintext only from creation and retains only metadata thereafter; it models Lumen-default selection and revoked token/grant state. Consent mock coverage validates a fixed ticket, bound details, safe local return mapping, malformed-ticket rejection, and the local tutorial route allowlist; the route test validates ticket storage, URL scrubbing, and bound API request. The focused selector passes 32 Vitest tests. Browser QA created and dismissed a one-time manage token, then loaded the consent page with its ticket scrubbed from the address bar and approved it into `/dashboard/account?tutorial=on` with session ticket cleared.
- [ ] 11. Synchronize configuration, documentation, and the publish gate: configuration/Kubernetes/example/site-config parity, Korean/English operational docs, focused and full verification, live smoke, production enablement, and final OpenSpec archive.

## Current Slice Evidence

- The public deployment/configuration slice is implemented but does **not** complete tasks 2–13: runtime and Helm now share the same effective MCP resource URL, including an explicit `mcp.public_url` or `app.public_api_base + "/api/v1/mcp"` fallback; Helm routes that resource and `/.well-known` discovery to the backend.
- Focused coverage passes for configured Streamable HTTP transport, OAuth URL derivation, grant limits, mounted account token issuance, configuration, and rendered Helm fallback paths. The MariaDB authority-cleanup target also passes.
- 2026-07-31: Task 2 authority completion is verified by the focused 19-test selector. Real MariaDB covers canonical dispatch/revoke serialization, `test_mcp_cleanup_reconciles_confirmed_deletion_after_late_dispatch_lease` (audit records `dispatch_lease_in_flight` and removes externally deleted credentials), idempotent concurrent confirmation, and atomic concurrent first-owner bootstrap.
- 2026-07-31: Tasks 3–4 OAuth/transport verification passes with 23 focused ASGI tests across `test_mcp_oauth.py` and `test_mcp_server.py`, plus the real-MariaDB refresh-replay transaction test. The OAuth reviewer approved the final `invalid_client`/401 `WWW-Authenticate` interoperability behavior for Authorization-header attempts.
- 2026-07-31: Task 9 completion is verified by the 68-test registry/domain selector. The expanded safe catalog adds Manila numeric quota, exact-project K3s list/get, Swift account aggregates, provider-bounded Barbican secret metadata, and SQL-bounded exact-project Waygate list/get. Registry inventory and feature-gate tests prove the closed classification, while adapter tests prove ownership checks and redaction of secret payloads, Barbican references, and Waygate connection details. Swift MCP reads now use strict fail-closed provider handling while retaining empty results for a missing account.
- 2026-07-31: Task 10 completion is verified by 32 focused Vitest tests and tutorial-browser account/consent flows. The browser-confirmed issued mock token is absent from the DOM after its one-time dialog closes; consent ticket query data is scrubbed before the decision, and approval returns locally with the session ticket cleared.
- 2026-07-31: Full verification completed after Task 9: `npm run test:all` passed 3,876 backend tests (66 skipped), 195 integration tests (17 skipped), and 926 frontend tests. `npm run lint:backend` passed `ruff check` but exited nonzero because its format check reports nine pre-existing unformatted chat files outside this slice. Independent MCP security review approved the Stage 2 catalog with no blocking findings. The OpenSpec remains active for unchecked Task 11; no commit, live smoke, production enablement, or archive is claimed.
- 2026-07-31: Local Compose activation completed without enabling production publish: `services.mcp=true`, direct backend resource `http://localhost:8000/api/v1/mcp`, and frontend consent `http://localhost:3080/oauth/mcp/authorize`. Applied idempotent migration 063 to the backend-configured database (all ten MCP tables present), restarted backend and frontend, and live-probed protected-resource and authorization-server metadata plus the transport’s unauthenticated 401 challenge. The restarted frontend SSR advertises `http://localhost:8000/api/v1/mcp`, not the non-proxied Vite origin. The account page redirects unauthenticated browser sessions to login; no real user token or OpenStack application credential was created during smoke verification.
- 2026-07-31: Final commit gate passed: `npm run test:all` passed 3,877 backend tests (66 skipped), 195 integration tests (17 skipped), and 928 frontend tests; `npm run lint:backend` passed `ruff check` and format checks after applying its nine reported format-only changes. Production-target images `ghcr.io/openstack-afterglow/afterglow-api:dev`, `ghcr.io/openstack-afterglow/afterglow:dev`, and `ghcr.io/openstack-afterglow/afterglow-worker:dev` rebuilt successfully from the final source tree.
