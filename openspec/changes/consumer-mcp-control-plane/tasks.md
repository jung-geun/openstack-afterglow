## Implementation Tasks

- [x] 1. Record the change and preserve rollout isolation: retained `mcp==1.28.1`, verified `dev`, preserved the pre-existing dirty worktree, and added the disabled `[services].mcp = false` rollout gate to config loading, generated K8s TOML, and both examples. Cross-change evidence is recorded in `builtin-ai-chat`; no shared chat task is marked complete.
- [ ] 2. Add the dedicated delegated-authority model: normalized tables, strict AEAD, owner-locked issuance/revocation/selection, browser management APIs, and the complete lease-guarded cleanup sweep. Partial evidence: `backend/tests/test_mcp_credentials.py` covers AEAD/PAT/scoped app credentials/orphan naming/route precedence; the database-gated OAuth replay proof is in `test_chat_db_integration.py`. Remaining §2 concurrency and cleanup-sweep coverage is in progress. The migration-ledger target remains independently blocked by pre-existing checksum drift in migration `016`.
- [ ] 3. Host a standards-compliant OAuth 2.1 authorization server in Stage 2: exact HTTPS resource/issuer metadata, DCR/CIMD validation, PKCE S256 authorization code plus rotating refresh tokens, strict resource/scope/redirect checks, and fail-closed revocation/refresh races. Partial evidence: unit protocol checks pass in `backend/tests/test_mcp_oauth.py`; the real-MariaDB refresh replay transaction test skips until `AFTERGLOW_TEST_DATABASE_URL` is configured.
- [ ] 4. Mount the current MCP transport with fail-closed request context: official stateless Streamable HTTP SDK transport at exact `/api/v1/mcp`, independent authentication, origin/host/media negotiation, bounded pagination/rates/concurrency, and explicit lifecycle management. Partial evidence: transport lifecycle/CORS/cursor coverage passes in `test_mcp_server.py`; Redis-backed per-grant limits fail closed and pass `test_mcp_limits.py`.
- [ ] 5. Create one canonical consumer-only registry and shared domain boundary: versioned closed schemas, grant-only principals, consumer adapters with strict ownership proofs, previews, safe outputs, audit, and no reflected HTTP/provider bypasses. Partial evidence: closed schemas, mutation envelope, bounded output serialization, operation reader, grant-scoped connection factory, safe quota adapter, and strict Nova/Cinder/Neutron read adapters are covered by `test_mcp_registry.py`, `test_mcp_core_tools.py`, `test_mcp_compute.py`, `test_mcp_storage.py`, and `test_mcp_network.py`. Remaining Stage 1 service adapters, cache invalidation, and per-domain audit paths are in progress.
- [ ] 6. Deliver the Stage 1 internal core cloud catalog: capability/operation/dashboard tools plus strictly owned Nova, Cinder, Neutron, Trove, and Zun read/mutation tools with idempotency, previews, redaction, audit, and policy fixes; keep public service disabled. Current internal catalog includes capabilities, operation metadata, fixed current-project cloud overview and quota projection, Nova list/get, parent-and-port-proven interface list, parent-and-child-proven volume attachment list, bounded lifecycle action, and non-force server delete; Cinder volume/snapshot/backup list/get plus non-force available-volume, snapshot, and backup deletes; Neutron network/subnet list/get plus owned non-shared network and subnet deletes; service-gated Trove instance list/get through verified connection project scope with bounded active-instance restart and state-gated delete; and service-gated Zun container list/get, bounded start/stop action, and stopped-only delete backed by raw owner proof. The remaining Stage 1 contracts and all public enablement remain incomplete.
- [ ] 7. Make mutation replay and asynchronous state explicit: validated idempotency keys and canonical argument hashing, atomic ledger claims/replay/tombstones, unknown send-boundary handling, owner-scoped operation reconciliation, and final preview/precondition revalidation. Partial evidence: `afterglow_vm_action`, `afterglow_vm_delete`, `afterglow_volume_delete`, `afterglow_volume_snapshot_delete`, `afterglow_volume_backup_delete`, `afterglow_network_delete`, `afterglow_subnet_delete`, `afterglow_database_instance_restart`, `afterglow_database_instance_delete`, `afterglow_container_action`, and `afterglow_container_delete` now use a durable ledger claim, mandatory idempotency validation before any provider preview, current-state previews before durable dispatch authorization, and terminal pre-dispatch failure persistence; adapter and transport regression coverage passes. Remaining provider mutation coverage and reconciliation paths are in progress.
- [ ] 8. Reuse the registry inside Lumen with durable v2 approval: server-owned registry provider, frozen opaque grant/selection snapshots, encrypted PostgreSQL v2 checkpointing, HMAC-bound typed approvals, final revalidation, and frontend chat approval lifecycle support. Partial evidence: worker support now includes protocol v2 while the default configured version remains 1; v2 activation still requires explicit configuration and fails run admission unless the lifecycle-owned encrypted PostgreSQL checkpointer is available. Run admission overwrites any client payload with an opaque encrypted snapshot of the selected grant ID, credential epoch, and selection generation; the worker never reselects it, and absent/malformed snapshots expose no Lumen cloud bindings. V2 resolves that snapshot into server-only registry bindings; its hashed binding fingerprint freezes grant ID, credential epoch, selection generation, registry version, and enabled-service fingerprint. Every execution revalidates that snapshot before audit, preview, the mutation send boundary, and connection construction; Lumen ledger rows use `source="lumen"`. Mutation replay identity is a UUIDv5 of the durable run ID and canonical tool-call ID, so retries replay only that call. Migration `065_chat_tool_approval_binding.sql` persists preview and decision fingerprints; the dispatch HMAC binds run owner/project, arguments, source/effect, tool/config/destination identity, preview fingerprint, and expiry, while the decision HMAC binds the decision actor and timestamp. Mutation previews flow into typed durable approval events. The chat now renders an accessible preview card exposing only redacted argument keys and posts explicit approve/deny decisions to the owner/project-scoped endpoint. Covered by `test_mcp_credentials.py`, `test_chat_agent_protocol.py`, `test_chat_graph_v2.py`, `test_chat_run_protocol.py`, `ChatToolApproval.test.ts`, and `ChatPanel.test.ts`. Encrypted checkpoint deployment proof and end-to-end database/browser coverage remain in progress.
- [ ] 9. Deliver the Stage 2 internal catalog and OAuth completion: close policy gaps and register every specified safe consumer service subset; omit disabled services and fail the inventory gate for unclassified consumer cloud routes.
- [ ] 10. Add account, OAuth-consent, and Lumen tutorial UX: account token/grant management, secure one-time plaintext handling, consent route with ticket binding and secure headers, Lumen starter prompts, and deterministic mock/browser coverage. Partial evidence: empty Lumen chats now offer five normal-message starter prompts (project inventory, VM planning, storage/network diagnosis, database preparation, and container preparation); a selection only fills/focuses the regular composer and never invokes a tool directly. `ChatWindow.test.ts`, `ChatPanel.test.ts`, and the design target pass. Account/OAuth browser and mock isolation coverage remain in progress.
- [ ] 11. Synchronize configuration, documentation, and the publish gate: configuration/Kubernetes/example/site-config parity, Korean/English operational docs, focused and full verification, live smoke, production enablement, and final OpenSpec archive.
