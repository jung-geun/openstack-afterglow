## Implementation Tasks

- [x] Add a workflow regression for consistent live OpenStack interface defaults.
- [x] Change the live test job fallback interface from `internal` to `public`.
- [x] Run orchestration checks and the required repository test/lint gates.
- [x] Archive the change with `--skip-specs`, commit, push, and verify replacement CI.
