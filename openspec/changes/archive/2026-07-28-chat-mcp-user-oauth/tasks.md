## Implementation Tasks

- [x] Add OAuth-required MCP classification and encrypted per-user connection persistence.
- [x] Implement SSRF-safe OAuth discovery, dynamic registration, PKCE authorization callback, and refresh.
- [x] Expose owner-bound connect, status, and disconnect APIs; never expose tokens.
- [x] Integrate active OAuth connections into MCP tool discovery and skip disconnected servers.
- [x] Add the chat extension connection UI and focused backend/frontend tests.
- [x] Run focused checks and an end-to-end mocked OAuth discovery smoke path.
