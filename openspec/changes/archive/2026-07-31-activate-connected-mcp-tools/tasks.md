## Implementation Tasks

- [x] Reproduce the empty frozen MCP snapshot for a default chat run.
- [x] Include active owner-visible extensions when selection is omitted without weakening agent allowlists or explicit opt-out.
- [x] Verify the default selection, live MCP discovery, and focused chat completion tests.
