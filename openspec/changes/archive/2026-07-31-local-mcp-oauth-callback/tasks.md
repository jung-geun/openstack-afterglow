## Implementation Tasks

- [x] Identify the distinct remote-MCP callback setting and reproduce the public-origin leak.
- [x] Permit only development loopback HTTP callbacks, with matching initiator-cookie security and regression tests.
- [x] Set the Compose backend return origin to the local frontend and cover the local callback completion URL.
- [x] Rebuild the backend, run focused and full test gates, and smoke-test the local callback route.
