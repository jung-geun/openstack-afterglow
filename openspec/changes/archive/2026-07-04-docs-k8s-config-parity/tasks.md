# Tasks

## Documentation
- [x] Update root README quickstarts for `afterglow.conf` and local dev secret behavior.
- [x] Update Korean deployment guide for config filename precedence, Docker Compose `.env`, K8s static manifests, generator, and Helm secret contracts.
- [x] Update English deployment guide with the same K8s/Helm secret contract.
- [x] Update static K8s template README with `afterglow.conf`, `afterglow-secrets`, worker parity, and production guardrails.
- [x] Clarify dev overlay and ArgoCD namespace prerequisites so root ConfigMap/Secret commands are production-only.

## Verification
- [x] Run targeted documentation consistency grep for stale config/secret guidance.
- [x] Run targeted config/K8s regression tests covering the documented behavior.
