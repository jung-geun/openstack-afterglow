## Implementation Tasks

- [x] Establish design authority in `DESIGN.md` and preserve existing primitive contracts.
- [x] Add scrim, layer, motion, and reduced-motion runtime and TypeScript token contracts.
- [x] Migrate named canonical overlays, controls, and motion consumers to semantic contracts.
- [x] Add source-contract and focused behavior tests for motion, layers, scrims, and the agent-instruction symlink.
- [x] Consolidate agent instructions in `AGENTS.md` and create `CLAUDE.md -> AGENTS.md`.
- [ ] Obtain passing repository validation gates (`npm run test:all` and `npm run lint:backend`), then archive this change.
- [x] Define mobile, tablet, and desktop responsive hierarchy contracts in `DESIGN.md` and require agents to consult them.
- [x] Make PageHeader action layout conform to the documented mobile hierarchy.
