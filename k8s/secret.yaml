# 시크릿은 아래 명령으로 직접 생성하세요 (이 파일을 Git에 커밋하지 마세요):
#
# kubectl create secret generic union-secrets \
#   --namespace=union \
#   --from-literal=OPENSTACK_PASSWORD=<실제 비밀번호> \
#   --from-literal=SECRET_KEY=$(python3 -c "import secrets; print(secrets.token_hex(32))")
#
# 또는 Sealed Secrets / External Secrets Operator를 사용하세요.
#
# 이 파일은 .gitignore에 포함되어 있습니다.
