apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: union
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'union-backend'
        static_configs:
          - targets: ['backend:8000']
        metrics_path: '/api/metrics'
