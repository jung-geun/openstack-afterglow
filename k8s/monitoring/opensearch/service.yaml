apiVersion: v1
kind: Service
metadata:
  name: opensearch
  namespace: afterglow
  labels:
    app: opensearch
spec:
  type: ClusterIP
  selector:
    app: opensearch
  ports:
    - port: 9200
      targetPort: 9200
      protocol: TCP
      name: http
