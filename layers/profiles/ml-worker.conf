# ml-worker 프로필 — ML/AI 워크로드용
# 한 줄에 레이어 하나. 위에서부터 lowerdir 우선순위 높음.
# 파일명 형식: <name>-latest.sqsh (심링크) 또는 <name>-<timestamp>.sqsh
#
# 빌드:
#   build-uv-python-layer.sh uvpy311 3.11 numpy scipy
#   → uvpy311-latest.sqsh (uv CPython 3.11 + uv 바이너리 + pip 패키지)
uvpy311-latest
cuda12-latest
devtools-latest
